import { templeAreasSupabaseService } from "./temple-areas.supabase.service";

// Export the Supabase service instance to act as the primary service
export const templeAreasService = templeAreasSupabaseService;
